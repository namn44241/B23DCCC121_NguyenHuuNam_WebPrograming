var express = require('express');
var app = express();

var mysql   = require('mysql');
var connect = mysql.createConnection({
    host    : 'localhost',
    user    : 'root',
    password: 'xyz',
});

app.get('/test',
        function (req, res){
            res.send("Hello world!\n");
        }
);

app.listen( 3666,
            function () {
                console.log("App listening on port 3666!\n");
            }
          );
